'use strict';

const message = document.querySelector('p');
const button = document.querySelector('button');

button.addEventListener('click', () => message.innerHTML = 'Mi primer click, ¡ole yo y la mujer que me parió!');